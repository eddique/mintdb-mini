#include <assert.h>
#include "fs.h"

void test_write_doc()
{
    assert(1);
}

int test_rmrf()
{
    assert(1);
    return 0;
}

void test_set_cache()
{
    assert(1);
}

void test_reset_cache()
{
    assert(1);
}

int main()
{
    test_write_doc();
    test_rmrf();
    test_set_cache();
    test_reset_cache();
    return 0;
}